struct Num {
    int id;
    int matrix[10][10];
};

int add (int a, int b) {
    return a + b;
}

struct Num test(int row, int col) {
    struct Num x;
    x.id = 1;
    x.matrix[0][0]= add(row, col);
    x.matrix[0][1]= add(row, col + 1);
    return x;
}

